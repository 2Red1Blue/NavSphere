# 设计检查

## 证据与方法

直接读取 openai/codex commit 20f109eadb9b45360e6ca4f1dee2e82c83a48f7a 的源码；DSH 本地 HEAD 682151187e46cfd8c44c457a491f08ff68c8d2fb，相关路径无本地改动。读取当前 Content OS 的执行、影子运行、协议 DDL 与路由配置。

两个分析请求同时经 codeagent-wrapper 发出，仅分析给定事实，不写源文件、不授权嵌套执行：

- codex session: 01a08622-fb70-7db0-97dc-1f74f1590400；正常返回。
- claude backend session: 7c8d698d-d22b-40c7-9082-d96baf99cb60；正常返回。没有取得可信上游模型身份证据，因此不将其描述成已验证的 Claude 模型独立审查通过。

本轮为纯设计文档，没有触发产品源码变更交付审查；上述是设计分析，不是代码双模型审查 PASS。

## 综合处理

采纳：取消与远端停止分开、unknown 不自动重调、先保存阶段产物、父子 usage 防重复、私有实验与严格发布身份门禁分离、工具权限不得随父进程无条件继承。

不采纳：仅依赖心跳判断任务超时；本地 fencing 归还可证明远端已停止；分析返回的假设 TS 文件补丁。这些均没有源码或协议保证。本方案保留外层执行时限，明确 native 会话可观测边界和远端 unknown。

修正先前结论：Codex 存在 context token_budget，也可向模型注入上下文提醒；这不证明逐调用财务预留有必要。此前拿 Agents SDK/工具 schema 代替 Codex 源码的证据范围不足，本设计以本轮读取的源码为准。

## 验证边界

仅检查 Markdown、引用路径、设计一致性和本轮提交范围。没有运行内容生产、产品测试、数据库迁移、推送或部署；不能宣称已解决实际生成问题或正式开放 v2 发布。
